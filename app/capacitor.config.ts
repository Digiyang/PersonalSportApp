import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rjiba.personalsportapp',
  appName: 'PersonalSportApp',
  webDir: 'dist'
};

export default config;
